<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- List View -->
    <record id="wompi_view_list" model="ir.ui.view">
        <field name="name">wompi.view.list</field>
        <field name="model">wompi</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="state"/>
                <field name="amount"/>
                <field name="date"/>
            </list>
        </field>
    </record>

    <!-- Form View -->
    <record id="wompi_view_form" model="ir.ui.view">
        <field name="name">wompi.view.form</field>
        <field name="model">wompi</field>
        <field name="arch" type="xml">
            <form string="Form">
                <header>
                    <button name="action_do_something" type="object" string="Do Something" class="oe_highlight"/>
                </header>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="state"/>
                        <field name="description"/>
                        <field name="amount"/>
                        <field name="date"/>
                        <field name="active"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Kanban View -->
    <record id="wompi_view_kanban" model="ir.ui.view">
        <field name="name">wompi.view.kanban</field>
        <field name="model">wompi</field>
        <field name="arch" type="xml">
            <kanban>
                <field name="name"/>
                <templates>
                    <t t-name="card">
                        <div class="oe_kanban_global_click">
                            <strong><field name="name"/></strong><br/>
                            <field name="state"/>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- Calendar View -->
    <record id="wompi_view_calendar" model="ir.ui.view">
        <field name="name">wompi.view.calendar</field>
        <field name="model">wompi</field>
        <field name="arch" type="xml">
            <calendar string="Calendar" date_start="date">
                <field name="name"/>
            </calendar>
        </field>
    </record>

    <!-- Pivot View -->
    <record id="wompi_view_pivot" model="ir.ui.view">
        <field name="name">wompi.view.pivot</field>
        <field name="model">wompi</field>
        <field name="arch" type="xml">
            <pivot>
                <field name="state"/>
                <field name="amount"/>
            </pivot>
        </field>
    </record>

    <!-- Search View -->
    <record id="wompi_view_search" model="ir.ui.view">
        <field name="name">wompi.view.search</field>
        <field name="model">wompi</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
                <field name="state"/>
                <filter string="Draft" name="state" domain="[('state','=','draft')]"/>
                <filter string="Active" name="active" domain="[('active','=',True)]"/>
            </search>
        </field>
    </record>

    <!-- Action -->
    <record id="action_wompi" model="ir.actions.act_window">
        <field name="name">Wompi</field>
        <field name="res_model">wompi</field>
        <field name="view_mode">list,form,kanban,calendar,pivot</field>
    </record>

    <!-- Menu -->
    <menuitem id="menu_wompi_root" name="Wompi"/>
    <menuitem id="menu_wompi_main" name="Records" parent="menu_wompi_root" action="action_wompi"/>
</odoo>
